# Active for Good - iOS/Android with React Native

## Development Setup Instructions
- Clone the repository above
- cd into the directory and run the following command to update submodules
`git submodule update --init --recursive`
